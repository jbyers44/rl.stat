<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Player Info</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://fonts.googleapis.com/css?family=Fira+Mono|Lilita+One|Shrikhand" rel="stylesheet" type="text/css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
  </head>
  <style>
  </style>
  <body>
    <div>
      <% include partials/navbar %>
    </div>
    <div class="content">
      <div class="sidebar">
        /  profile information
        <div class="playerpanel">
          <% if(profile !== null){ %>
          <img class="avatar" src=<%="" + profile.avurl%> alt="avatar"/>
          <div class="info">
            <div class="t1"><%=profile.name%></div>
            <ul class="detail">
              <li class="cat">platform:</li>
              <li>steam</li>
            </ul>
            <ul class="detail2">
              <li class="cat"><a href =<%=profile.link%>>profile link</a></li>
            </ul>
          </div>
          <% } %>
        </div>
      </div>
      <div class="main">
        /  rank information
        <div class="rankpanels">
          <div class="panel">
            <div class="t1">
              standard
            </div>
            <% if(ranks !== null){ %>
              <div>
                <%=ranks.standard.rankString%>
                <%=ranks.standard.divisionString%>
              </div>
              <div>
                <img class="rankpic" src=<%="/images/site/" + ranks.standard[1] + ".png"%> alt="rank"/>
              </div>
              <div>
              rating: <%=ranks.standard[0]%>
              </div>
            <% } %>
            <% if(error !== null){ %>
              <%= error %>
            <% } %>
          </div>
          <div class="panel">
            <div class="t1">
              doubles
            </div>
            <% if(ranks !== null){ %>
              <div>
                <%=ranks.doubles.rankString%>
                <%=ranks.doubles.divisionString%>
              </div>
              <div>
                <img class="rankpic" src=<%="/images/site/" + ranks.doubles[1] + ".png"%> alt="rank"/>
              </div>
              <div>
              rating: <%=ranks.doubles[0]%>
              </div>
            <% } %>
            <% if(error !== null){ %>
              <%= error %>
            <% } %>
          </div>
          <div class="panel">
            <div class="t1">
              duel
            </div>
            <% if(ranks !== null){ %>
              <div>
                <%=ranks.duel.rankString%>
                <%=ranks.duel.divisionString%>
              </div>
              <div>
                <img class="rankpic" src=<%="/images/site/" + ranks.duel[1] + ".png"%> alt="rank"/>
              </div>
              <div>
              rating: <%=ranks.duel[0]%>
              </div>
            <% } %>
            <% if(error !== null){ %>
              <%= error %>
            <% } %>
          </div>
          <div class="panel">
            <div class="t1">
              solo standard
            </div>
            <% if(ranks !== null){ %>
              <div>
                <%=ranks.solo.rankString%>
                <%=ranks.solo.divisionString%>
              </div>
              <div>
                <img class="rankpic" src=<%="/images/site/" + ranks.solo[1] + ".png"%> alt="rank"/>
              </div>
              <div>
              rating: <%=ranks.solo[0]%>
              </div>
            <% } %>
            <% if(error !== null){ %>
              <%= error %>
            <% } %>
          </div>
        </div>
      </div>
    </div>
    <% include partials/footer %>
  </body>
</html>